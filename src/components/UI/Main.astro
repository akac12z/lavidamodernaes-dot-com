---
import Shuffle from "./button/Shuffle.tsx";
---

<main class="overflow-x-hidden">
  <section
    class="bg-rosa-moderdonia px-2 pt-6 sm:pt-10 pb-10 border-b-4 border-cutre-black mx-auto flex flex-col items-center justify-center text-balance"
  >
    <h1 class="text-center text-cutre-white mb-4 text-4xl">¡Gora Moderdona!</h1>
    <p class="text-lg text-center font-semibold">
      Esto es un homenaje a todas esas frases de <span
        class="underline decoration-wavy font-bold tracking-wider"
        >#LaVidaModerna es</span
      > y al programa que tanto nos divirtió y que tantos ofendiditos creó.
    </p>
    <span
      class="text-lg mt-8 text-cutre-black bg-cutre-white px-2 py-1 border-4 border-cutre-black transform rotate-12 font-bold"
    >
      Keep it Cutre ✨
    </span>
  </section>

  {/* quote's section */}
  <slot />

  <section class="py-8 text-center flex items-center justify-center">
    <h2
      class="px-2 py-1 border-4 border-cutre-blackb bg-rosa-moderdonia text-lg"
    >
      No hay más frases <span class="text-xs">(por ahora) </span>
    </h2>
  </section>

  <Shuffle client:idle />
</main>
